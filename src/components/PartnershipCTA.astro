---
import Modal from './Modal.astro';
---

<section class="partnership-cta">
  <div class="cta-content">
    <h2>Devenez Partenaire</h2>
    <p>Vous souhaitez collaborer avec nous ? Contactez-nous pour discuter des opportunités de partenariat.</p>
    <button class="cta-button" data-modal-open data-modal-target="partnershipModal">Nous Contacter</button>
  </div>
</section>

<Modal id="partnershipModal" title="Contactez-nous">
  <form name="partnership" method="POST" data-netlify="true" class="partnership-form">
    <div class="form-group">
      <label for="email">Email</label>
      <input type="email" id="email" name="email" required />
    </div>
    <div class="form-group">
      <label for="message">Message</label>
      <textarea id="message" name="message" rows="4" required></textarea>
    </div>
    <button type="submit" class="submit-button">Envoyer</button>
  </form>
</Modal>

<style>
  .partnership-cta {
    background: rgb(var(--accent));
    color: white;
    padding: 4rem 2rem;
    text-align: center;
    margin: 4rem -1rem;
  }

  .cta-content {
    max-width: 800px;
    margin: 0 auto;
  }

  .cta-content h2 {
    color: white;
    margin-bottom: 1rem;
  }

  .cta-content p {
    margin-bottom: 2rem;
    font-size: 1.2rem;
  }

  .cta-button {
    background: white;
    color: rgb(var(--accent));
    padding: 1rem 2rem;
    border: none;
    border-radius: 8px;
    font-size: 1.1rem;
    font-weight: 500;
    cursor: pointer;
    transition: all 0.3s ease;
  }

  .cta-button:hover {
    background: rgb(var(--bg-secondary));
    transform: translateY(-2px);
  }

  .partnership-form {
    margin-top: 2rem;
  }

  .form-group {
    margin-bottom: 1.5rem;
  }

  .form-group label {
    display: block;
    margin-bottom: 0.5rem;
    color: rgb(var(--text-primary));
  }

  .form-group input,
  .form-group textarea {
    width: 100%;
    padding: 0.75rem;
    border: 1px solid rgb(var(--border-color));
    border-radius: 8px;
    font-size: 1rem;
  }

  .submit-button {
    background: rgb(var(--accent));
    color: white;
    padding: 0.75rem 1.5rem;
    border: none;
    border-radius: 8px;
    font-size: 1rem;
    cursor: pointer;
    transition: background-color 0.3s ease;
  }

  .submit-button:hover {
    background: rgb(var(--accent-dark));
  }
</style>